(()=>{const el=document.querySelector('.explorer');if(!el||typeof L==='undefined')return;const id=el.dataset.itemId;const accepted=el.dataset.accepted==='1';const map=L.map('map',{zoomControl:true,worldCopyJump:false}).setView([40.6,14.2],7);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);let overlay=null;if(accepted){overlay=L.tileLayer(`/catalog/${id}/tiles/{z}/{x}/{y}`,{maxZoom:Number(el.dataset.maxzoom||22),minZoom:Number(el.dataset.minzoom||0),opacity:.92,errorTileUrl:''}).addTo(map);}let bounds=null;try{const b=JSON.parse(el.dataset.bounds);if(b&&b.length===4){bounds=L.latLngBounds([b[1],b[0]],[b[3],b[2]]);map.fitBounds(bounds);L.rectangle(bounds,{weight:1,fill:false,dashArray:'5 6'}).addTo(map);}}catch(_e){}document.getElementById('overlayToggle')?.addEventListener('change',e=>{if(!overlay)return;e.target.checked?overlay.addTo(map):map.removeLayer(overlay)});document.getElementById('fit')?.addEventListener('click',()=>bounds&&map.fitBounds(bounds));map.on('click',e=>{L.popup().setLatLng(e.latlng).setContent(`${e.latlng.lat.toFixed(5)}, ${e.latlng.lng.toFixed(5)}`).openOn(map);});})();
