# OpenLayers plugin

`serverLayer()` creates an ordinary XYZ layer. `clientLayer()` intercepts tile loading, retrieves DNT1, renders it in-browser, and assigns the resulting PNG object URL to the OpenLayers tile image.
